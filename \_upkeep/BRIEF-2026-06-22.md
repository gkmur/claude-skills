# Upkeep brief - 2026-06-22

Repo is clean and structurally sound. Two stale paths left over from the `claude-skills` -> `skills` rename (2026-06-11) were auto-fixed. README and CLAUDE.md are both behind on listing the newer plugins (5 added since they were last updated); those need a content update, which is a proposal.

## Auto-applied

- `README.md` line 8: `gkmur/claude-skills` -> `gkmur/skills` in the install command. The old repo name no longer exists; one unambiguous replacement.
- `plugins/skill-improver/skills/improve-skill/SKILL.md` line 38: `~/projects/claude-skills/` -> `~/projects/skills/` in the skill-lookup path list. Same rename drift; `~/projects/claude-skills` does not exist.

## Proposals

1. **README.md - Plugins section is 5 plugins behind.** (README.md:14-29) Only lists `pretext-typography` and `moodboard`. Missing: `session-state`, `rubin`, `writing-voice`, `skill-improver`, `maintainer-orchestrator`. The README is the public face of the marketplace; someone installing via it would not know the other skills exist. Risk: adding content, not removing - safe but judgment call on what level of detail each entry gets.

2. **CLAUDE.md - Architecture bullet list is 2 plugins behind.** (CLAUDE.md:17-22) Lists 5 plugins but is missing `writing-voice` and `maintainer-orchestrator`, both added in the last two commits. Low risk to add two bullets, but it's adding new content. Risk: minimal.

3. **CLAUDE.md - `rubin-workspace/` reference.** (CLAUDE.md:23) Lists `rubin-workspace/` as a scratch dir "not published", but it does not exist in the working tree (it is gitignored). The note still serves as a warning about the gitignored path, so this may be intentional - flag for Gabe to confirm it is still relevant.
