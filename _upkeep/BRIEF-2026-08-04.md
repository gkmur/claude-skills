Repo's clean mechanically - no broken links, no trailing whitespace, no dupe blocks. Real drift is CLAUDE.md and README.md both undercounting plugins: 3 shipped plugins (adhd, maintainer-orchestrator, writing-voice) are missing from both docs' plugin lists, and CLAUDE.md's intro line still claims all skill dirs are `gabe-`prefixed when moodboard's isn't. No auto-fixes applied - this needs a doc rewrite, not a mechanical patch.

## Auto-applied
none

## Proposals
1. **CLAUDE.md Architecture section (lines 20-27) is missing 3 of 8 plugins.** `plugins/adhd/`, `plugins/maintainer-orchestrator/`, and `plugins/writing-voice/` all exist, ship, and are listed correctly in `.claude-plugin/marketplace.json`, but never got added to CLAUDE.md's per-plugin bullet list. Risk: low, but anyone reading CLAUDE.md to understand repo structure gets a wrong picture of what's here.
2. **CLAUDE.md line 2 claims skill dirs are uniformly `gabe-`prefixed and names `gabe-moodboard` - but the actual dir is `plugins/moodboard/skills/moodboard/` (no prefix).** Same wrong name repeats on line 32. Either the dir should be renamed to match the doc, or the doc's claim should be loosened to "usually gabe-prefixed" - that's a call for Gabe, not this pass.
3. **README.md's Plugins section only documents pretext-typography and moodboard** - the other 6 (adhd, maintainer-orchestrator, rubin, session-state, skill-improver, writing-voice) aren't described anywhere in README, only in marketplace.json. Lower priority than #1 since marketplace.json is the actual install source of truth, but README is what a human skimming the repo sees first.
