Repo's clean, no broken links or mechanical junk. Only drift is docs lagging the two newest plugins (adhd, maintainer-orchestrator) - nothing auto-fixed, both proposals below need your call since they're content additions, not mechanical.

## Auto-applied
none

## Proposals
1. **CLAUDE.md is stale on plugin inventory** - `CLAUDE.md:2` and `CLAUDE.md:20-26`. The intro line and Architecture bullet list only name 6 of the 8 plugin dirs; `plugins/adhd/` and `plugins/maintainer-orchestrator/` (both added 2026-08-03/04 per git log) are missing from both, and `gabe-adhd` is missing from the skill-dir list on line 2. Risk: low, but it's a content addition (new bullets/names), not a mechanical fix, so left for you.
2. **README.md only documents 2 of 8 plugins** - `README.md`. Install section and Plugins list cover just `pretext-typography` and `moodboard`; `.claude-plugin/marketplace.json` already has all 8 correctly described. README predates the moodboard v1.0.0 release commit and everything after. Risk: low (marketplace.json is the source of truth to copy from), but again a real content add, not mechanical - flagging rather than writing your public-facing README copy for you.
3. **CLAUDE.md "Out of bounds" references a directory that doesn't exist** - `CLAUDE.md:36`, `rubin-workspace/`. Not present anywhere in the repo (only referenced in `.gitignore:5`). Either it was never created or got cleaned up already. Not touching since removing a rule needs your sign-off, but worth a quick check on whether the rule is still needed.
