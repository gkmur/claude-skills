Repo's clean. One mechanical fix (missing newline). Same two docs-lag proposals as yesterday, still unaddressed - your call, not mechanical.

## Auto-applied
- Added missing trailing newline to `plugins/adhd/hooks/hooks.json`.

## Proposals
1. **CLAUDE.md is stale on plugin inventory** - `CLAUDE.md:2` and `CLAUDE.md:20-26`. Intro line and Architecture bullets only name 6 of 8 plugin dirs; `plugins/adhd/` and `plugins/maintainer-orchestrator/` (added 2026-08-03/04) are missing, and `gabe-adhd` is missing from the skill-dir list on line 2. Content addition, not mechanical - left for you.
2. **README.md only documents 2 of 8 plugins** - `README.md`. Install section and Plugins list cover just `pretext-typography` and `moodboard`; `.claude-plugin/marketplace.json` already has all 8 correctly described and is the copy-source. Content add, not mechanical.
3. **CLAUDE.md "Out of bounds" references `rubin-workspace/`, which doesn't exist** - `CLAUDE.md:36`. Only mentioned in `.gitignore:5`. Either never created or already cleaned up. Worth a quick check on whether the rule's still needed - not touching since removing a rule needs your sign-off.
