Repo's got real drift this pass, all in the docs layer, not the skill code. Nothing was safe to auto-fix mechanically -- every issue needs a rewrite decision, so it's all proposals. `.claude-plugin/marketplace.json` is accurate and current (8 plugins); the drift is root CLAUDE.md/README.md/AGENTS.md and one skill's SKILL.md falling behind.

## Auto-applied
None.

## Proposals

1. **Root CLAUDE.md Architecture section is missing 3 of 8 plugins.** `CLAUDE.md:20-26` lists only moodboard, pretext-typography, session-state, rubin, skill-improver -- missing `adhd`, `maintainer-orchestrator`, `writing-voice` (all present in `.claude-plugin/marketplace.json` and `plugins/`). Opening line (`CLAUDE.md:2`) also claims all skill dirs are `gabe-`prefixed, but `plugins/moodboard/skills/moodboard/` and `plugins/adhd/skills/adhd/` are not. Risk if unfixed: any agent reading CLAUDE.md undercounts the marketplace and mis-states the naming convention.

2. **CLAUDE.md references a `rubin-workspace/` dir that doesn't exist.** `CLAUDE.md:26` (Architecture) and `CLAUDE.md:36` (Out of bounds) both describe `rubin-workspace/` as an existing scratch dir; it's not present anywhere in the repo (confirmed via `find`). Either it was cleaned up and the doc never updated, or it never got committed -- worth asking Gabe which, since "Out of bounds" protection for a dir that's already gone is dead weight either way.

3. **CLAUDE.md's moodboard description is stale -- describes a removed dependency.** `CLAUDE.md:22` says moodboard "requires local wiki + qmd MCP"; `CLAUDE.md:32` repeats "need[s] the wiki MCP / Pinterest shadow at `~/wiki/pinterest/`". `README.md:21-25` says the same. But `plugins/moodboard/README.md` now states "Local files only. No services, no APIs, no dependencies," and the skill's `allowed-tools` frontmatter has no MCP tools -- just Bash/Read/Write/Edit/Glob/Grep/AskUserQuestion. Moodboard was rebuilt (see the 2026-07-xx commits: "rebuild around influence dial + local-only retrieval") and root docs never caught up. This is also inconsistent with this same CLAUDE.md's own line 2, which still calls it `gabe-moodboard` when the actual skill dir is just `moodboard`.

4. **README.md's Plugins section only documents 2 of 8 plugins.** `README.md` "## Plugins" covers pretext-typography and moodboard only -- missing rubin, session-state, skill-improver, adhd, maintainer-orchestrator, writing-voice. Since `marketplace.json` already has accurate one-line descriptions for all 8, this could mostly be a copy-over rather than fresh writing.

5. **AGENTS.md points at a "Codex-specific" section in CLAUDE.md that doesn't exist.** `AGENTS.md:3`: "Codex-specific notes are in its '## Codex-specific' section" -- grepped CLAUDE.md, no such heading. Either the section got deleted from CLAUDE.md at some point, or AGENTS.md was written speculatively and the section never got added.

6. **gabe-writing-voice SKILL.md has a stale absolute path to a workflow file that no longer exists.** `plugins/writing-voice/skills/gabe-writing-voice/SKILL.md:126` calls `Workflow({ scriptPath: "/Users/gabrielmurray/.claude/skills/gabe-writing-voice/workflow/trope-pipeline.js", ... })`. That file doesn't exist anywhere on disk (`find` came up empty) -- looks like a leftover from before the skill moved to `~/wiki/skills/custom/gabe-writing-voice/`. This breaks the skill's own heavy-pass pipeline (used for cover letters, work emails). Flagging but NOT touching: this SKILL.md is a symlink target living at `~/wiki/skills/custom/gabe-writing-voice/`, outside this repo's working directory, so it's out of scope for this pass regardless of risk tier.

7. **gabe-writing-voice: advertised Slack-mechanics section isn't wired into the skill body.** `plugin.json` and `.claude-plugin/marketplace.json` both headline "a deep-evidence Slack mechanics section distilled from ~3,100 of his real sent messages... with register shifts for peers vs reports vs leadership vs broadcast" as the differentiator. `references/slack-patterns.md` (12KB) exists but SKILL.md never loads or references it -- only `voice-profile.md` and `ai-writing-tropes.md` are cited. The advertised feature may be silently unused. Same out-of-repo caveat as #6.
