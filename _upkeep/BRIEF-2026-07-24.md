# Upkeep brief - 2026-07-24

Repo is clean, no mechanical fixes needed this pass (no trailing whitespace, no missing final newlines, no broken in-repo links, no duplicate blocks). Same four proposals carry over unchanged from the 2026-07-23 brief - nothing new drifted, nothing resolved. All four still need your call.

## Auto-applied

None.

## Proposals

1. **README.md - Plugins section still lists only 2 of 7 plugins.** (README.md:14-29) Documents `pretext-typography` and `moodboard` only. Missing `session-state`, `rubin`, `writing-voice`, `skill-improver`, `maintainer-orchestrator` - all live in `plugins/` with accurate descriptions already sitting in `.claude-plugin/marketplace.json`. Flagged 2026-06-25, still open. Risk: additive content, judgment call on per-entry detail level.

2. **CLAUDE.md - Architecture bullet list still missing 2 plugins.** (CLAUDE.md:21-27) Lists 5 plugins, omits `writing-voice` and `maintainer-orchestrator` (both published, and both already named correctly in the header line at CLAUDE.md:2). Flagged 2026-06-25, still open. Risk: minimal, two bullets matching existing pattern.

3. **gabe-improve-skill SKILL.md - stale marketplace name, fourth time flagged.** (plugins/skill-improver/skills/gabe-improve-skill/SKILL.md:99) "If the target lives in the `claude-skills` marketplace" - renamed to `gkmur` 2026-06-11. Inside backticks, so mechanically un-fixable per this pass's rules. Flagged 2026-06-24, 2026-06-25, and 2026-07-23, still open every time. Risk: near zero, one-word noun swap.

4. **gabe-writing-voice: dead hardcoded path + orphaned Slack-patterns file.** `plugins/writing-voice/skills/gabe-writing-voice` is a symlink into `~/wiki/skills/custom/gabe-writing-voice/` (the wiki repo, not this one - any actual edit has to happen there, out of this pass's scope).
   - SKILL.md:126 hardcodes `scriptPath: "/Users/gabrielmurray/.claude/skills/gabe-writing-voice/workflow/trope-pipeline.js"`. That directory does not exist - the real `~/.claude/skills` entry is named `gabriel-writing-voice` (different symlink, pointing at a `gabriel-writing-voice` wiki dir that also doesn't exist). The actual script lives at `~/wiki/skills/custom/gabe-writing-voice/workflow/trope-pipeline.js`, reachable through this repo's own symlink. The hardcoded path bypasses that and will fail if the workflow step ever runs as written.
   - `plugins/writing-voice/.claude-plugin/plugin.json:2` (this file IS tracked in this repo, not symlinked) advertises "a deep-evidence Slack mechanics section... distilled from ~3,100 of his real sent messages... with register shifts for peers vs reports vs leadership vs broadcast." `references/slack-patterns.md` exists with that content but SKILL.md's body never references it anywhere (only `voice-profile.md` and `ai-writing-tropes.md` are wired in) - it's dead weight the skill doesn't use.
   Flagged 2026-07-23, still open. Risk: the path fix is mechanical but lands outside this repo (wiki repo); the plugin.json/SKILL.md mismatch needs a judgment call on whether to wire the file in or trim the claim.

Note: `rubin-workspace/` (referenced in CLAUDE.md:27,36) is not present on this checkout - this is expected, it's gitignored local scratch space (`.gitignore:5`), not drift.
