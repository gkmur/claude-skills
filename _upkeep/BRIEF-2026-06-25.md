# Upkeep brief - 2026-06-25

Repo is in good shape. Found 15 markdown files missing final newlines - auto-applied all. The rubin-workspace/ proposal from prior briefs is closed: .gitignore confirms the directory is intentionally absent from the tracked tree (gitignored eval output), so CLAUDE.md's warning about it is load-bearing, not stale. Three other proposals from prior passes remain open and unchanged.

## Auto-applied

- Added missing final newline to 15 markdown files: README.md, AGENTS.md, CLAUDE.md, and all 12 SKILL.md/reference/template markdown files under plugins/. None of these had content changes - one byte appended to each. Standard posix compliance.

## Proposals

1. **README.md - Plugins section lists only 2 of 7 plugins.** (README.md:14-29) Only documents `pretext-typography` and `moodboard`. Missing: `session-state`, `rubin`, `writing-voice`, `skill-improver`, `maintainer-orchestrator`. The marketplace.json at `.claude-plugin/marketplace.json` has accurate descriptions for all 7 - copy from there. Risk: additive content change, judgment call on detail level per entry.

2. **CLAUDE.md - Architecture bullet list missing 2 plugins.** (CLAUDE.md:21-26) Lists 5 plugins but omits `writing-voice` and `maintainer-orchestrator`, both present and published in `plugins/`. The header line (CLAUDE.md:2) already names all 8 gabe-prefixed skills correctly. Risk: minimal - two bullet additions matching existing pattern.

3. **gabe-improve-skill SKILL.md - stale marketplace name in backticks.** (plugins/skill-improver/skills/gabe-improve-skill/SKILL.md:99) Reads "the `claude-skills` marketplace" - the marketplace was renamed to `gkmur` in June 2026 (see CLAUDE.md:2). Change to `gkmur` or "this marketplace". Inside backticks, cannot auto-fix. Risk: very low - one-word noun, meaning-preserving.

4. **RESOLVED - rubin-workspace/ references are intentional, not stale.** (.gitignore:5) Prior briefs flagged CLAUDE.md:27 and CLAUDE.md:36 as referencing a missing directory. The .gitignore entry explicitly labels it "Skill eval/benchmark output - local only" and suppresses it from tracking. The CLAUDE.md warning ("do not commit") is load-bearing. No change needed.
