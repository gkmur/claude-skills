Repo is clean mechanically - no broken links, no trailing whitespace, no missing newlines, no dupe blocks. Two long-open README gaps are still open, plus one new contradiction in marketplace.json worth a look.

## Auto-applied
none

## Proposals
1. **`.claude-plugin/marketplace.json`'s maintainer-orchestrator description contradicts its own `plugin.json`.** `.claude-plugin/marketplace.json:24` still says "Run a single orchestrator session that wakes on a heartbeat... directs one background worker agent per repo... Claude Code port of steipete's Codex maintainer loop: /loop 5m heartbeat..." - this is the multi-repo orchestrator that was removed 2026-08-13 (commit 11ebc62). `plugins/maintainer-orchestrator/.claude-plugin/plugin.json:2` already has the correct, current description ("Triage a single repo's GitHub queue... The multi-repo orchestrator loop was removed on 2026-08-13"). marketplace.json's `keywords` array also still lists `orchestrator`, `loop`, `background-agents`, `autonomous`, `steipete` - all describing the removed feature. Risk: low to fix (copy plugin.json's description/keywords over), but it changes a marketplace-facing description so leaving to you. New since 08-13; not in any prior brief.

2. **(carried over since 2026-06-22, still open) README.md is stale against the actual plugin lineup.** `README.md:9,15-19` still lists `pretext-typography` (deleted 2026-08-14) as an installable plugin with its own section, and the Install/Plugins sections never mention `adhd` or `maintainer-orchestrator` (both live since 2026-06-23/06-11). `.claude-plugin/marketplace.json` has accurate one-line descriptions for all 3 current plugins - straightforward copy-source. This has been flagged in every brief since 06-22 and never actioned; flagging once more rather than rewriting your public README copy for you.
