Repo's clean mechanically - no dead links, no trailing whitespace, no missing newlines, nothing to auto-fix. Real drift is in the two hand-written plugin inventories (README.md, CLAUDE.md) - both fell behind `.claude-plugin/marketplace.json`, which is accurate and complete (all 7 plugins). Needs your call since it's content, not a typo.

## Auto-applied
none

## Proposals

1. **README.md `## Plugins` section only documents 2 of 7 plugins** (pretext-typography, moodboard) - missing session-state, rubin, skill-improver, maintainer-orchestrator, writing-voice.
   - Where: README.md:13-29
   - Why: `.claude-plugin/marketplace.json` has correct, current descriptions for all 7 - README could just mirror those (shortened) instead of drifting further each time a plugin is added.
   - Risk: low - additive only, but it's real content so left for you to review the copy.

2. **CLAUDE.md `## Architecture` bullet list is missing 2 of 7 plugin dirs** (`plugins/maintainer-orchestrator/`, `plugins/writing-voice/`).
   - Where: CLAUDE.md:20-27
   - Why: same root cause as #1 - the list wasn't updated when those two plugins were added (maintainer-orchestrator: 21fee13; writing-voice: 4d6573a/19f481b). The top-line skill-dir list on CLAUDE.md:2 is already accurate, just the Architecture bullets lag.
   - Risk: low - two bullets to add, no wording changes elsewhere needed.
