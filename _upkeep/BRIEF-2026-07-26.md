Still mechanically clean - no dead links, whitespace, or newline issues, nothing auto-fixed. The README/CLAUDE.md plugin-list drift from yesterday's brief is still unresolved (untouched since). Found one new thing: every plugin's `.claude-plugin/plugin.json` description has drifted from what's in `.claude-plugin/marketplace.json` - the plugin.json copies are newer and more detailed, marketplace.json is stale.

## Auto-applied
none

## Proposals

1. **`marketplace.json` descriptions are stale vs each plugin's own `plugin.json`** (all 7 plugins differ, not just wording - moodboard and pretext-typography plugin.json descriptions describe capabilities the marketplace.json copy omits, e.g. moodboard's semantic-search-across-boards behavior and pretext's multi-slot layouts).
   - Where: `.claude-plugin/marketplace.json` (root) vs `plugins/*/.claude-plugin/plugin.json`
   - Why: `marketplace.json` was last touched 2026-06-11; all 7 `plugin.json` files were revised 2026-06-23 with sharper descriptions that never got mirrored back up. marketplace.json is what shows in the plugin listing/install UI, so that's the one showing stale copy.
   - Risk: low-medium - it's descriptive text, not a name change, but description content can affect trigger/matching, so left for you per the "don't touch descriptions" rule.

2. **(carried over, still open) README.md `## Plugins` section only documents 2 of 7 plugins** (pretext-typography, moodboard) - missing session-state, rubin, skill-improver, maintainer-orchestrator, writing-voice.
   - Where: README.md:13-29
   - Risk: low.

3. **(carried over, still open) CLAUDE.md `## Architecture` bullet list is missing 2 of 7 plugin dirs** (`plugins/maintainer-orchestrator/`, `plugins/writing-voice/`).
   - Where: CLAUDE.md:20-27
   - Risk: low.
