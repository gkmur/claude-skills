Repo is clean mechanically - no trailing whitespace, missing newlines, or broken links. Yesterday's plugin pruning (0a9abf3, 11ebc62) cut the marketplace from 8 plugins down to 3 (adhd, moodboard, maintainer-orchestrator) and left the top-level docs and marketplace.json out of sync with that new reality. No auto-fixes this pass - everything found is a content/meaning change.

## Auto-applied
none

## Proposals

1. **marketplace.json still advertises a capability that was removed in the last commit.** `.claude-plugin/marketplace.json` maintainer-orchestrator entry says it "wakes on a heartbeat, triages every personal repo's queue, and directs one background worker agent per repo" (multi-repo loop). But `plugins/maintainer-orchestrator/.claude-plugin/plugin.json` was updated in the same session (commit 11ebc62, this morning) to say: "The multi-repo orchestrator loop was removed on 2026-08-13; this plugin is now the per-repo triage skill only." marketplace.json is what installers see first and it's now overselling the plugin. Risk: low to fix (just copy plugin.json's current wording over), but it's a description/meaning edit so flagging rather than auto-applying.

2. **Root CLAUDE.md's Architecture section and opening line describe 5 plugins that no longer exist on disk.** `CLAUDE.md:2` lists skill dirs `gabe-pretext-typography`, `gabe-wrap-up`, `gabe-rubin`, `gabe-improve-skill`, `gabe-orchestrate` - all deleted in 0a9abf3/11ebc62. `CLAUDE.md:20-27` still documents `plugins/pretext-typography/`, `plugins/session-state/`, `plugins/rubin/`, `plugins/skill-improver/`, `rubin-workspace/` - none of these exist anymore (`plugins/` only has `adhd`, `maintainer-orchestrator`, `moodboard`). This was flagged in prior briefs (2026-08-08, 2026-08-10 through 08-13) when the dirs still existed but were undocumented; now it's inverted - the docs describe dirs that are gone. Needs a rewrite of that whole section, not a patch - your call on how much detail per current plugin.

3. **README.md's Plugins section still only covers pretext-typography and moodboard** - pretext-typography is now deleted, and adhd + maintainer-orchestrator (the two live plugins besides moodboard) are undocumented. This is the same finding repeated in every brief since 2026-06-22, now sharper: one of the two documented plugins doesn't exist anymore. `.claude-plugin/marketplace.json` (once #1 above is fixed) would be the accurate copy-source.

4. **CLAUDE.md "Out of bounds" note (line 36) and `.gitignore:5` still reference `rubin-workspace/`, guarding a directory for a plugin (`rubin`) that no longer exists at all.** Previously this was "gitignored but load-bearing since rubin might use it"; now the rubin plugin itself is gone, so the warning has no plugin left to serve. Confirm whether to drop both the CLAUDE.md line and the .gitignore entry, or keep as dead weight on purpose.
