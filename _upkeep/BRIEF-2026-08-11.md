No repo changes since the 2026-08-10 pass (git log confirms zero commits in between). The 7 doc-drift proposals from BRIEF-2026-08-08.md are all still open and still accurate -- re-verified against current file contents this pass, including a full re-check of README.md, CLAUDE.md, AGENTS.md, all 8 plugin.json/SKILL.md files, and a repo-wide sweep for trailing whitespace, missing final newlines, duplicate lines, broken relative links, and stale absolute paths. Nothing mechanically safe to auto-fix.

## Auto-applied
None.

## Proposals
See `_upkeep/BRIEF-2026-08-08.md` items 1-7 -- unchanged, unresolved, now carried four passes with no action:
1. Root CLAUDE.md Architecture section missing adhd, maintainer-orchestrator, writing-voice; opening line's "all gabe-prefixed" claim is wrong (moodboard's skill dir is `moodboard`, not `gabe-moodboard`).
2. CLAUDE.md references `rubin-workspace/` which doesn't exist on disk.
3. CLAUDE.md/README.md moodboard description is stale (still says "requires wiki + qmd MCP"; moodboard is now local-only).
4. README.md Plugins section documents 2 of 8 plugins.
5. AGENTS.md points at a "## Codex-specific" CLAUDE.md section that doesn't exist.
6. gabe-writing-voice SKILL.md has a dead absolute path to `trope-pipeline.js` (out of repo scope -- lives in `~/wiki/skills/custom/gabe-writing-voice/`).
7. gabe-writing-voice: advertised Slack-mechanics section (`references/slack-patterns.md`) isn't loaded/referenced by SKILL.md (same out-of-repo caveat).

These are all judgment calls (rewriting doc sections, not mechanical fixes), which is why the daily pass keeps declining to touch them. If you want them cleared, say so explicitly and the next pass will apply items 1-5 (in-repo, low-risk) directly.
